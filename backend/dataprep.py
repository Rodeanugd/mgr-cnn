from imports import *
import directories as dirs
import filesplit as fls
import spectograms as spc
import datasplit as dts

genres = 'blues classical country disco hiphop jazz metal pop reggae rock'
genres = genres.split()

# dirs.makedirs(genres)
# fls.splitfiles(genres)
# spc.generate_spectograms(genres)
# dts.splitdata(genres)

